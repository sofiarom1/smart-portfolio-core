[project]
name = "smart-portafolio-core"
version = "1.0.0"
description = "poetry en el proyecto de smart prolfolio"
authors = [
    {name = "Laura Sofia Romero",email = "laurasofiaromero1003@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)",
    "pytest-cov (>=7.0.0,<8.0.0)"
]
